const en=document.getElementById("enabled"),list=document.getElementById("list"),ed=document.getElementById("editor"),name=document.getElementById("name"),prompt=document.getElementById("prompt");let edit=null;
async function load(){let s=await chrome.storage.local.get({enabled:true,customPrompts:[]});en.checked=s.enabled;render(s.customPrompts)}
function render(ps){list.innerHTML=ps.length?ps.map(p=>`<div class="item"><b>${safe(p.name)}</b><small>${safe(p.prompt).slice(0,80)}</small><button data-del="${p.id}">Delete</button><button data-edit="${p.id}">Edit</button></div>`).join(""):"<small>No custom prompts yet.</small>";list.querySelectorAll("[data-del]").forEach(b=>b.onclick=()=>del(b.dataset.del));list.querySelectorAll("[data-edit]").forEach(b=>b.onclick=()=>editPrompt(b.dataset.edit))}
function safe(s){return s.replace(/[&<>"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c]))}
en.onchange=()=>chrome.storage.local.set({enabled:en.checked});
document.getElementById("add").onclick=()=>{edit=null;name.value="";prompt.value="";ed.classList.remove("hidden")};document.getElementById("cancel").onclick=()=>ed.classList.add("hidden");
document.getElementById("save").onclick=async()=>{if(!name.value.trim()||!prompt.value.trim())return;let s=await chrome.storage.local.get({customPrompts:[]}),a=s.customPrompts;if(edit)a=a.map(p=>p.id===edit?{...p,name:name.value.trim(),prompt:prompt.value.trim()}:p);else a.push({id:crypto.randomUUID(),name:name.value.trim(),prompt:prompt.value.trim()});await chrome.storage.local.set({customPrompts:a});ed.classList.add("hidden");render(a)};
async function del(id){let s=await chrome.storage.local.get({customPrompts:[]}),a=s.customPrompts.filter(p=>p.id!==id);await chrome.storage.local.set({customPrompts:a});render(a)}
async function editPrompt(id){let s=await chrome.storage.local.get({customPrompts:[]}),p=s.customPrompts.find(x=>x.id===id);edit=id;name.value=p.name;prompt.value=p.prompt;ed.classList.remove("hidden")}load();